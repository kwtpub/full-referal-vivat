# 👤 Как дать пользователю права администратора

## 📋 Процесс назначения админа

### Шаг 1: Пользователь должен зарегистрироваться

Пользователь должен:
1. Зайти на сайт
2. Зарегистрироваться через форму регистрации
3. Активировать аккаунт по ссылке из email

**Важно:** Пользователь должен быть зарегистрирован и активирован перед назначением админом.

### Шаг 2: Назначить права администратора

После регистрации пользователя, выполните одну из команд ниже.

---

## 🐳 Вариант 1: Через Docker контейнер (рекомендуется)

### Если проект запущен через Docker Compose:

```bash
# Выполнить команду внутри контейнера server
docker compose exec server npx ts-node src/scripts/makeAdmin.ts user@example.com
```

**Пример:**
```bash
docker compose exec server npx ts-node src/scripts/makeAdmin.ts admin@vivat.com
```

### Если используется docker-compose.prod.yml:

```bash
docker compose -f docker-compose.prod.yml exec server npx ts-node src/scripts/makeAdmin.ts user@example.com
```

---

## 💻 Вариант 2: Локально (без Docker)

Если вы запускаете проект локально:

```bash
cd server
npx ts-node src/scripts/makeAdmin.ts user@example.com
```

**Пример:**
```bash
cd server
npx ts-node src/scripts/makeAdmin.ts admin@vivat.com
```

---

## 🔍 Проверка результата

После выполнения команды вы должны увидеть:

```
✅ Пользователь Имя Пользователя (user@example.com) теперь админ
```

Если пользователь не найден, вы увидите ошибку:
```
❌ Ошибка: ...
```

**Возможные причины ошибки:**
- Пользователь с таким email не зарегистрирован
- Email указан неверно (проверьте регистр и опечатки)
- База данных недоступна

---

## 📝 Пример полного процесса

```bash
# 1. Пользователь регистрируется на сайте
#    Email: john@example.com

# 2. Пользователь активирует аккаунт по ссылке из email

# 3. Вы назначаете его админом:
docker compose exec server npx ts-node src/scripts/makeAdmin.ts john@example.com

# 4. Результат:
# ✅ Пользователь John Doe (john@example.com) теперь админ

# 5. Пользователь перезаходит в систему и получает доступ к админ-панели
```

---

## 🛠 Альтернативный способ (через Prisma Studio)

Если у вас есть доступ к Prisma Studio:

```bash
# Запустить Prisma Studio
docker compose exec server npx prisma studio

# Или локально:
cd server
npx prisma studio
```

Затем:
1. Откройте браузер на `http://localhost:5555`
2. Найдите таблицу `Agent`
3. Найдите пользователя по email
4. Измените поле `isAdmin` на `true`
5. Сохраните изменения

---

## ⚠️ Важные замечания

1. **Email должен быть точным** - проверьте регистр и опечатки
2. **Пользователь должен быть зарегистрирован** - нельзя назначить админом несуществующего пользователя
3. **Права админа даются сразу** - после выполнения команды пользователь получает полный доступ
4. **Перезаход не обязателен** - но рекомендуется для обновления токенов

---

## 🔐 Что может делать админ

Админ имеет доступ к:
- Управлению всеми агентами
- Просмотру всех клиентов
- Подтверждению закрытых сделок
- Начислению бонусов
- Активации/деактивации агентов

---

## 🆘 Решение проблем

### Ошибка: "Cannot find module"

Убедитесь что вы находитесь в правильной директории или используете Docker:

```bash
# Проверьте что контейнер запущен
docker compose ps

# Проверьте что вы в корне проекта
pwd
```

### Ошибка: "User not found"

1. Проверьте что пользователь зарегистрирован:
   ```bash
   # Через Prisma Studio или SQL запрос
   docker compose exec db mysql -u app_user -p referral_vivat
   SELECT email, name, isAdmin FROM Agent;
   ```

2. Проверьте правильность email (регистр важен!)

### Ошибка: "Database connection failed"

Проверьте что база данных запущена:

```bash
docker compose ps db
```

---

## 📚 Дополнительная информация

- Скрипт находится в: `server/src/scripts/makeAdmin.ts`
- Поле `isAdmin` в таблице `Agent` в базе данных
- По умолчанию все пользователи создаются с `isAdmin: false`

