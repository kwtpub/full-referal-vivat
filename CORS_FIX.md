# 🔧 Исправление ошибок CORS и 404

## Проблемы

1. **CORS ошибка**: `XMLHttpRequest cannot load https://bandboats.ru/api/registration due to access control checks`
2. **404 ошибка**: Изображение `logo-03 1.png` не найдено

## ✅ Решения

### 1. Проверьте настройки в `.env`

Убедитесь что в `.env` файле указан правильный домен:

```bash
CLIENT_URL=https://bandboats.ru
API_URL=https://bandboats.ru/api
VITE_API_URL=https://bandboats.ru/api
```

**Важно:**
- Используйте `https://` если у вас настроен SSL
- Используйте `http://` если SSL еще не настроен
- Домен должен быть точным (без слэша в конце)

### 2. Перезапустите контейнеры

После изменения `.env`:

```bash
docker compose down
docker compose up -d --build
```

Или для production:

```bash
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml up -d --build
```

### 3. Проверьте логи

```bash
# Логи сервера (для проверки CORS)
docker compose logs server | grep -i cors

# Логи nginx
docker compose logs nginx

# Все логи
docker compose logs -f
```

### 4. Проверьте что контейнеры запущены

```bash
docker compose ps
```

Все контейнеры должны быть в статусе `Up` или `Up (healthy)`.

---

## 🔍 Дополнительная диагностика

### Проверка CORS на сервере

Подключитесь к контейнеру сервера и проверьте переменные окружения:

```bash
docker compose exec server env | grep CLIENT_URL
```

Должно показать:
```
CLIENT_URL=https://bandboats.ru
```

### Проверка API напрямую

Попробуйте сделать запрос напрямую к API:

```bash
curl -I https://bandboats.ru/api/registration
```

Или через браузер откройте:
```
https://bandboats.ru/api/registration
```

### Проверка изображений

Проверьте что файл существует:

```bash
# В контейнере клиента
docker compose exec client ls -la /usr/share/nginx/html/images/

# Или локально
ls -la client/public/images/
```

---

## 🛠 Если проблема сохраняется

### Вариант 1: Временно разрешить все origins (только для отладки!)

В `server/src/index.ts` временно измените:

```typescript
app.use(
  cors({
    credentials: true,
    origin: true, // Разрешить все origins (только для отладки!)
    methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS'],
    allowedHeaders: ['Content-Type', 'Authorization'],
  })
);
```

**⚠️ ВНИМАНИЕ:** Это небезопасно для production! Используйте только для отладки.

### Вариант 2: Проверьте что nginx правильно проксирует запросы

Убедитесь что в `nginx/nginx.conf` правильно настроен proxy_pass:

```nginx
location /api {
    proxy_pass http://backend;  # Должно быть backend, не server:3000
    ...
}
```

### Вариант 3: Проверьте DNS и SSL

Если используете HTTPS:
1. Убедитесь что SSL сертификат настроен правильно
2. Проверьте что домен указывает на правильный IP
3. Проверьте что порты 80 и 443 открыты

```bash
# Проверка DNS
nslookup bandboats.ru

# Проверка портов
sudo ufw status
```

---

## 📝 Исправление 404 для изображений

Если изображение `logo-03 1.png` не найдено:

1. Проверьте что файл существует в `client/public/images/`
2. Проверьте путь в коде - возможно используется неправильное имя файла
3. Пересоберите клиент:

```bash
cd client
npm run build
cd ..
cp -r client/dist client-build
docker compose restart nginx
```

---

## ✅ Чек-лист исправления

- [ ] `.env` содержит правильный `CLIENT_URL=https://bandboats.ru`
- [ ] `.env` содержит правильный `VITE_API_URL=https://bandboats.ru/api`
- [ ] Контейнеры перезапущены после изменения `.env`
- [ ] Все контейнеры запущены (`docker compose ps`)
- [ ] Nginx правильно проксирует `/api` запросы
- [ ] SSL сертификат настроен (если используете HTTPS)
- [ ] Порты 80 и 443 открыты в firewall
- [ ] Изображения находятся в правильной директории

---

## 🆘 Если ничего не помогло

1. Проверьте полные логи:
```bash
docker compose logs --tail=100
```

2. Проверьте конфигурацию nginx:
```bash
docker compose exec nginx nginx -t
```

3. Проверьте что сервер отвечает:
```bash
docker compose exec server curl http://localhost:3000/api/health
```

4. Создайте issue с полными логами и конфигурацией
